<%= form_with(model: todo_list, class: 'p-5') do |form| %>

  
  <% if todo_list.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</p>

      <ol>
        <% todo_list.errors.each do |error| %>
          <li class="text-danger"><%= error.full_message %></li>
        <% end %>
      </ol>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control'%>
  </div>
  

  <div class="actions">
    <%= form.submit class: "btn btn-primary"%>
  </div>
<% end %>
